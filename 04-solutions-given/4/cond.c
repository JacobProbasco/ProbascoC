#include <stdio.h>
main()
{
	int a = 5;
	int b = 10;
	int result;

	result = a > b ? a : b;

/* It's just (strange) syntax!!!!  The ? and the : separate the three 
 * expressions.  If first expression is TRUE, then result = a.  If not
 * then result = b.
 */
}

Tell me if that helps.



